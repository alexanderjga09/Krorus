[package]
name = "exif_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "exif_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
kamadak-exif = "0.5"